// This line essentially puts the whole app into "SPA mode" (https://kit.svelte.dev/docs/single-page-apps) which is a requirement for
// deploying to an environment such as github pages.
export const ssr = false;

// Pages can be instructed to pre-render where possible (e.g. if the page just consists of static content)
export const prerender = true;
